services:
  - type: web
    name: tts-web-app
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_TTS_API_KEY
        sync: false
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
